<section class="">
	<div class="grid-container">
		<div class="grid-x {% if block.settings.cta-h3-align == 'right' %}text-right{% elsif block.settings.cta-h3-align == 'center' %}text-center{% elsif block.settings.cta-h3-align == 'left' %}text-left{% endif %} align-center">
			<div class="small-8 medium-12 cell">
				{% if block.settings.cta-h3 != blank %}
					<h3>{{ block.settings.cta-h3 | escape }}</h3>
				{% endif %}
				{% if block.settings.cta-p != blank %}
			        <p>{{ block.settings.cta-p | escape }}</p>
			    {% endif %}
			</div>
			<div class="small-6 cell cta-pad">
			  {% form 'customer' %}
		      {{ form.errors | default_errors }}
		      {% if form.posted_successfully? %}
				    <p class="form--success">{{ 'general.newsletter_form.confirmation' | t }}</p>
		      {% else %}
				<form>
				  <div class="grid-x grid-padding-x">
				  	<div class="small-12 cell">
				  		<input type="hidden" name="contact[tags]" value="newsletter">
				  	</div>
				    <div class="small-12 medium-8 cell">
				        <input type="email" name="contact[email]" id="Email" value="{% if customer %}{{ customer.email }}{% endif %}" placeholder="{{ 'general.newsletter_form.email_placeholder' | t }}" autocorrect="off" autocapitalize="off" placeholder="Email">
				    </div>
				    <div class="small-12 medium-4 cell">
				        <button type="submit" class="secondary button" name="commit" id="Subscribe">{{ 'general.newsletter_form.submit' | t }}</button>
				    </div>
				  </div>
				</form>
				{% endif %}
    			{% endform %}
			</div>
		</div>
	</div>
</section>
